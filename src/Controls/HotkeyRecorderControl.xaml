<UserControl x:Class="SemaphURL.Controls.HotkeyRecorderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Focusable="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Border x:Name="HotkeyBorder"
                Grid.Column="0"
                Background="{ui:ThemeResource ControlFillColorDefaultBrush}"
                BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10,6"
                Cursor="Hand"
                PreviewMouseLeftButtonDown="Border_MouseLeftButtonDown">
            <TextBlock x:Name="HotkeyText"
                       VerticalAlignment="Center"
                       FontFamily="Segoe UI"
                       FontSize="13"
                       IsHitTestVisible="False" />
        </Border>

        <ui:Button x:Name="ResetButton"
                   Grid.Column="1"
                   Icon="{ui:SymbolIcon ArrowReset24}"
                   ToolTip="Reset to default"
                   Appearance="Secondary"
                   Margin="8,0,0,0"
                   Click="ResetButton_Click"
                   Visibility="{Binding ShowResetButton, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
